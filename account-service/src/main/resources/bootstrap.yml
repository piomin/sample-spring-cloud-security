spring:  
  application:
    name: account-service
  cloud:
    config:
      failFast: true
      uri: https://localhost:8888

encrypt:
  keyStore:
    location: classpath:/discovery.jks
    password: 123456
    alias: discovery
    secret: 123456